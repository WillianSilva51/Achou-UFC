DB_HOST=db
DB_PORT=5432
DB_NAME=achou_ufc
DB_USER=achou_user
DB_PASS=achou_pass
DB_SSLMODE=disable

JWT_SECRET=dev-achou-ufc-jwt-secret-change-me-2026-local
JWT_EXPIRATION=2592000
CORS_ORIGIN=http://localhost:8000,http://localhost:3000,http://127.0.0.1:3000,http://localhost:5500,http://127.0.0.1:5500,http://localhost:5501,http://127.0.0.1:5501

# Opcional. Se ficar vazio, o backend libera o reCAPTCHA apenas para desenvolvimento local.
RECAPTCHA_SECRET_KEY=

# Supabase Storage usado para anexos de fotos dos itens.
SUPABASE_URL=
# Use a chave server-side do projeto: service_role JWT ou sb_secret_*. Nunca use sb_publishable_* aqui.
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=fotos-itens
